{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cluck-in.example/schemas/v1/ai-decision.schema.json",
  "title": "AIDecision",
  "description": "Structured message classification returned by AI. The app decides which actions to issue.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "messageId",
    "decision",
    "relevance",
    "urgency",
    "reason"
  ],
  "properties": {
    "messageId": {
      "type": "string",
      "minLength": 1,
      "description": "Exact id of the ExternalMessage being classified."
    },
    "decision": {
      "type": "string",
      "enum": [
        "urgent",
        "allow",
        "hold"
      ],
      "description": "urgent: recommend immediate attention; allow: eligible for normal delivery; hold: recommend deferred delivery."
    },
    "relevance": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Relevance to the current task, from 0 (none) to 1 (highest)."
    },
    "urgency": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Time sensitivity, from 0 (none) to 1 (highest)."
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "description": "Short human-readable explanation, never a command to execute."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional normalized extension fields. Do not include raw provider API objects or credentials."
    }
  }
}
